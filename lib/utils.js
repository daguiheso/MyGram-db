'use strict'

// exportando como una propiedad
const utils = {
  // extractTags: extractTags
  // con ecma6 al tener una propiedad igual o con el mismo nombre al valor puedo evitar esta duplicidad
  extractTags
}

function extractTags (tetx) {
  return []
}

module.exports = utils
